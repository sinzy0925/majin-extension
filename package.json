{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^17.2.3",
    "rimraf": "^6.0.1"
  }
}
